import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  container: {
    borderRadius: 60,
    backgroundColor: 'var(--pantone)',
    display: 'flex',
  },
  cardImage: {
    width: 100,
  },
}));
